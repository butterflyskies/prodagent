[package]
name = "prodagent-config"
version.workspace = true
description = "Three-tier configuration cascade for prodagent — embedded defaults, user, and project layers with figment"
readme = "../../README.md"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }

[dependencies]
agent-command-knowledge = { workspace = true }
agent-policy = { workspace = true }
camino = { workspace = true }
figment = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
tempfile = "3"
